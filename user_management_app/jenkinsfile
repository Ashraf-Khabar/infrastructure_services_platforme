pipeline {
    agent {
        label 'dotnet_slave_agent'
    }
    stages {
        stage('Update Packages') {
            steps {
                sh 'sudo apt update -y'
                sh 'sudo apt upgrade -y'
            }
        }

        stage('Install Dependencies') {
            steps {
                sh 'ls -li'
            }
        }
    }

}