pipeline {
    agent any
    
    stages {
        stage('Install Python') {
            steps {
                sh '''
                    apt update
                    apt install -y python3
                    python3 --version
                    apt install -y python3-pip
                    pip3 --version
                '''
            }
        }
    }
}