pipeline {
    agent {
        any {
            label 'dotnet'
        }
    }
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build') {
            steps {
                sh 'dotnet --version'
                sh 'dotnet build'
            }
        }
    }
}