pipeline {
    agent any
    
    stages {
        stage('Install Python') {
            steps {
                sh '''
                    sudo apt update
                    sudo apt install python3
                    python3 --version
                    sudo apt install python3-pip
                    pip3 --version
                '''
            }
        }
    }
}