pipeline {
    agent {
        docker {
            image 'python:3.9-slim'
            reuseNode true
        }
    }
    
    stages {
        stage('Check Python') {
            steps {
                sh '''
                    python3 --version
                    pip3 --version
                '''
            }
        }
    }
}