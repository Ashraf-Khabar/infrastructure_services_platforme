pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Au lieu de sudo apt update
                sh 'apt update'
                
                // Ou utiliser le chemin complet avec permissions
                sh '/usr/bin/apt update'
            }
        }
    }
}